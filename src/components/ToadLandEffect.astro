<style>
  .emoji {
    top: 0;
    position: fixed;
    font-size: 24px;
    z-index: 1000; /* Смайлики будут поверх всех элементов */
    /* Временно убираем анимацию для диагностики */
  }
  @keyframes explosion {
    0% {
      opacity: 1;
      transform: translate(0, 0) scale(1);
    }
    100% {
      opacity: 0;
      transform: translate(var(--x), var(--y)) scale(0.5);
    }
  }
</style>
<script>
  document.addEventListener("astro:page-load", () => {
    const highlightElement = document.querySelector(".about-highlight");

    if (highlightElement) {
      highlightElement.addEventListener("mouseenter", function (event) {
        console.log("Наведение на элемент ToadLand");

        const rect = highlightElement.getBoundingClientRect();
        const centerX = rect.left + rect.width / 2;
        const centerY = rect.top + rect.height / 2;

        console.log("Координаты центра:", centerX, centerY);

        for (let i = 0; i < 20; i++) {
          createEmoji(centerX, centerY);
        }
      });
    } else {
      console.error("Элемент .about-highlight не найден!");
    }

    function createEmoji(x, y) {
      const emoji = document.createElement("div");
      emoji.innerHTML = "🐸💖";
      emoji.classList.add("emoji");
      document.body.appendChild(emoji);

      // Простое позиционирование без анимации
      emoji.style.left = `${x}px`;
      emoji.style.top = `${y}px`;

      console.log("Смайлик добавлен на позиции:", x, y);

      // Убираем смайлик через 3 секунды
      setTimeout(() => {
        emoji.remove();
      }, 3000);
    }
  });
</script>
